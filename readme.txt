Use with caution ! Always copy a backup before using

Usage: 

Pass in sourcedir path which contains directories that hold images. All directories inside the sourcedir path will have their contents (images) converted. If remove is
set to true, all unrelated (non .webp extention files, including folder) will be DELETED !! 